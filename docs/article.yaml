slug: pagina
title: pagina
form: docs
status: published
category: documentation
tags: [documentation, static-site, markdown, kineglyph, typescript]
description: >-
  A documentation article is a folder of markdown. pagina renders it to a static site, packs it
  into one portable file, and drops it into a host page without bringing a design system along.
author: pagina contributors
site_url: https://nano112.github.io/pagina/

# No `cover:`. There is no image in this repository that honestly represents the project — the
# only committed artwork is a test fixture's placeholder gradient — and a cover invented for the
# occasion is worse than none: it is the first thing a reader sees and the first thing a social
# card shows. The figures on the pages are drawn from real scenes instead.
#
# Which is exactly the case social cards exist for: with no cover, every page of this article used
# to share as a line of blue text. Every one of them now shares as a drawn card instead. There is
# nothing to write to get that — the block below is the default spelt out, kept because this is the
# article that documents the setting and an article that does not exercise its own contract is a
# contract nobody believes. See `social-cards.md`.
og:
  template: editorial
  scheme: light

kineglyph:
  # No `theme:` either, and that is the point rather than an omission. An article that declares
  # no Kineglyph theme is painted by whatever `--pg-*` tokens its host has set, so these figures
  # follow this page — light, dark, or a host's own palette. It is the contract `theming.md`
  # describes, exercised by the article that describes it.
  #
  # `widths` rather than `width`: every figure is drawn four times, and the page shows the drawing
  # measured for the width its frame actually has. A diagram cannot reflow in the browser the way a
  # paragraph can — SVG has no text wrapping, so geometry is measured once, here — which is why one
  # drawing is wrong on a phone: it can only be scaled, and scaling this page's 960px figures into
  # a 326px column takes their 12px labels to 4px. Four drawings cost a couple of KB gzipped a page and
  # buy a figure that is readable at full size on a handset instead of one to be scrolled sideways.
  widths: [960, 640, 440, 320]

snippets:
  # `..` is the repository root. The pages below quote pagina's real source — the token contract
  # from the stylesheet that ships it, this article's own `article.yaml` — instead of a copy that
  # can drift. A snippet that has gone stale is a build failure, not a wrong sentence.
  roots: [".", ".."]

# Not article content, and not published. `design/` and `plans/` are dated working documents kept
# in the repository on purpose (see "Where the design notes went" in article-folder.md); naming
# them here is what the folder's own documentation tells everyone else to do, and an article that
# does not follow its own contract is a contract nobody believes.
exclude:
  - design/
  - plans/

nav:
  - { title: Why pagina, page: index.md }
  - { title: How it works, page: how-it-works.md }
  - { title: Install, page: install.md }
  - section: The contracts
    children:
      - { title: The article folder, page: article-folder.md }
      - { title: A blog, page: blog.md }
      - { title: Theming, page: theming.md }
  - section: Writing
    children:
      - { title: The editor, page: editing.md }
      - { title: Try the editor, page: demo.md }
  - section: Publishing
    children:
      - { title: Article bundles, page: bundles.md }
      - { title: Hosting, page: hosting.md }
      - { title: Deploying, page: deploying.md }
      - { title: Social cards, page: social-cards.md }
      - { title: Search, page: search.md }
