node_modules/
dist/
.pagina/
e2e/.tmp/
test-results/
playwright-report/
.pagina-scratch/

# The docs site built by tools/build-docs-site.sh, and the bundle beside it.
site/
site.pgz
.release/

# The article `examples/node-host/server.mjs` seeds for itself on first run: a pack of `docs/` and
# the folder it unpacks into. Both are build output of the repository's own content.
examples/node-host/.article/
examples/node-host/.article.pgz

# `wrangler dev` keeps its local R2 and Durable Object state here.
examples/worker-r2/.wrangler/
